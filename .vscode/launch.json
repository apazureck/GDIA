{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "gdb",
            "request": "launch",
            "name": "Launch Program",
            "target": "out.exe",
            "cwd": "${workspaceRoot}/bin",
            "terminal": ""
        }
    ]
}